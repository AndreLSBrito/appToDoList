import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    padding: 70,
    alignItems: "center",
    alignContent:"center",
    backgroundColor: '#0D0D0D',
    height: 169
  }
});